interface:
  display_name: "Yy"
  short_description: "Repo-specific patterns and workflows for yy"
  default_prompt: "Use the yy repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true